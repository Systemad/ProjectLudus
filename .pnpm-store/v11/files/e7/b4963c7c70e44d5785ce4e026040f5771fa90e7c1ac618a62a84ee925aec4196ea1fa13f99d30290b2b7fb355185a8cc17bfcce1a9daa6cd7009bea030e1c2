export declare function cropImageAsync({ imagePath, outputPath, x, y, width, height, }: {
    imagePath: string;
    outputPath: string;
    x: number;
    y: number;
    width: number;
    height: number;
}): Promise<void>;
/**
 * Resize the image to a maximum size and return the resized image in buffer
 * Since tunnel allow only 1MB of payload, we need to resize the image to a maximum size
 */
export declare function resizeImageToMaxSizeAsync(imagePath: string, { maxBytes, // 700KB (consider base64 and JSON overhead)
maxWidth, // initial resize cap
minWidth, // stop shrinking past this
startQuality, // initial JPEG quality
minQuality, // don’t go below this
qualityStep, // lower quality by this amount
downscaleStep, // shrink width by 10% if quality floor is reached
maxRuns, }?: {
    maxBytes?: number | undefined;
    maxWidth?: number | undefined;
    minWidth?: number | undefined;
    startQuality?: number | undefined;
    minQuality?: number | undefined;
    qualityStep?: number | undefined;
    downscaleStep?: number | undefined;
    maxRuns?: number | undefined;
}): Promise<{
    bytes: number;
    quality: number;
    width: number;
    buffer: Buffer;
}>;
//# sourceMappingURL=imageUtils.d.ts.map