{{ config(materialized="view") }}

select g.id as game_id, t.id as alternative_name_id, t.game as igdb_game_fk

from {{ source("igdb_source_20251229083704", "games__alternative_names") }} t

inner join {{ source("igdb_source_20251229083704", "games") }} g on t._dlt_parent_id = g._dlt_id
