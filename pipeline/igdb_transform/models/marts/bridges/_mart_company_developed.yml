version: 2
models:
    - name: mart_company_developed

      config:
          alias: company_developed
          contract: { enforced: true }

      constraints:
          - type: primary_key
            columns: [company_id, game_id]

          - type: foreign_key
            columns: [company_id]
            to: ref('mart_companies')
            to_columns: [id]

          - type: foreign_key
            columns: [game_id]
            to: ref('mart_games')
            to_columns: [id]

      description: ""
      columns:
          - name: company_id
            data_type: bigint
            description: ""
          - name: game_id
            data_type: bigint
            description: ""
