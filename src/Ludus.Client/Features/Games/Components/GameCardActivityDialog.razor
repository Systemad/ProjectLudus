@using Ludus.Shared
<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h4">
            @Game.Name
        </MudText>
    </TitleContent>
    <DialogContent>
        <MudGrid>
            <MudItem xs="12">
            <!--
                <MudSelect @bind-Value="selectedGameStatus" Label="Select Status" Variant="Variant.Outlined" T="GameStatus">
                    @foreach (var status in Enum.GetValues<GameStatus>())
                    {
                        <MudSelectItem Value="@status">@status</MudSelectItem>
                    }
                    <MudSelectItem Value="@("Burger")">Burger</MudSelectItem>
                </MudSelect>
            -->
            </MudItem>
        </MudGrid>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Cancel</MudButton>
        <MudButton Color="Color.Primary" OnClick="Submit">Save</MudButton>
    </DialogActions>
</MudDialog>

