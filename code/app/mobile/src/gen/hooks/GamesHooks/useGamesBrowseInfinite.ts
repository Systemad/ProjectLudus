/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { InfiniteData, QueryKey, QueryClient, InfiniteQueryObserverOptions, UseInfiniteQueryResult } from '@tanstack/react-query'
import type { RequestConfig, ResponseErrorConfig } from '../../.kubb/client'
import type { GamesBrowseOptions, GamesBrowseQuery, GamesBrowseStatus200, GamesBrowseStatus400 } from '../../types/GamesBrowse'
import { infiniteQueryOptions, useInfiniteQuery } from '@tanstack/react-query'
import { gamesBrowse } from '../../clients/gamesBrowse'

export const gamesBrowseInfiniteQueryKey = ({ query }: Omit<GamesBrowseOptions, 'headers'> = {}) => [{ url: '/catalog/games/browse' }, ...(query ? [query] : [])] as const

type GamesBrowseInfiniteQueryKey = ReturnType<typeof gamesBrowseInfiniteQueryKey>

export function gamesBrowseInfiniteQueryOptions({ query }: GamesBrowseOptions = {}, config: Partial<Omit<RequestConfig, 'path' | 'query' | 'body' | 'headers' | 'url'>> = {}) {
  const queryKey = gamesBrowseInfiniteQueryKey({ query })
  return infiniteQueryOptions<GamesBrowseStatus200, ResponseErrorConfig<GamesBrowseStatus400>, InfiniteData<GamesBrowseStatus200>, typeof queryKey, NonNullable<GamesBrowseQuery['Page']>>({
    queryKey,
    queryFn: async ({ signal, pageParam }) => {
      query = {
        ...(query ?? {}),
        ['Page']: pageParam as unknown as GamesBrowseQuery['Page'],
      } as GamesBrowseQuery
      const { data } = await gamesBrowse({ ...config, query, signal: config.signal ?? signal, throwOnError: true })
      return data
    },
    initialPageParam: 1,
    getNextPageParam: (lastPage) => lastPage?.['nextPage']
  })
}

/**
 * @summary Browse games
 * {@link /catalog/games/browse}
 */
export function useGamesBrowseInfinite<TQueryFnData = GamesBrowseStatus200, TError = ResponseErrorConfig<GamesBrowseStatus400>, TData = InfiniteData<TQueryFnData>, TQueryKey extends QueryKey = GamesBrowseInfiniteQueryKey, TPageParam = NonNullable<GamesBrowseQuery['Page']>>({ query }: { query?: GamesBrowseOptions['query'] | (() => GamesBrowseOptions['query']) } = {}, options: {
  query?: Partial<InfiniteQueryObserverOptions<TQueryFnData, TError, TData, TQueryKey, TPageParam>> & { client?: QueryClient },
  client?: Partial<Omit<RequestConfig, 'path' | 'query' | 'body' | 'headers' | 'url'>>
} = {}) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const resolvedParams = { query: typeof query === 'function' ? query() : query }
  const queryKey = resolvedOptions?.queryKey ?? gamesBrowseInfiniteQueryKey(resolvedParams)

  const queryResult = useInfiniteQuery({
   ...gamesBrowseInfiniteQueryOptions(resolvedParams, config),
   ...resolvedOptions,
   queryKey,
  } as unknown as InfiniteQueryObserverOptions<TQueryFnData, TError, TData, TQueryKey, TPageParam>, queryClient) as UseInfiniteQueryResult<TData, TError> & { queryKey: TQueryKey }

  queryResult.queryKey = queryKey as TQueryKey

  return queryResult
}