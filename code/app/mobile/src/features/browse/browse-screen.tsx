import { EmptyState } from "@/shared/ui/screen-state";

export default function BrowseScreen() {
  return (
    <EmptyState
      title="Lists are coming next"
      message="Global game lists will replace the category browser."
    />
  );
}
