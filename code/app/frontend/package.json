{
    "name": "frontend",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vp run game-index#build",
        "ready": "vp fmt && vp lint && vp run test -r && vp run build -r",
        "dev": "vp run game-index#dev",
        "prepare": "vp config"
    },
    "devDependencies": {
        "vite-plus": "^0.1.24"
    },
    "engines": {
        "node": ">=22.12.0"
    },
    "packageManager": "pnpm@11.3.0"
}
