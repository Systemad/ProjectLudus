/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
    Client,
    RequestConfig,
    ResponseErrorConfig,
} from "../../../client.ts";
import type {
    QueryKey,
    QueryClient,
    QueryObserverOptions,
    UseQueryResult,
} from "@tanstack/react-query";
import type {
    SearchingQueryResponse,
    SearchingQueryParams,
} from "../../models/Searching.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";
import { searching } from "../../clients/searching.ts";

export const searchingQueryKey = (params: SearchingQueryParams = {}) =>
    ["v1", { url: "/api/search" }, ...(params ? [params] : [])] as const;

export type SearchingQueryKey = ReturnType<typeof searchingQueryKey>;

export function searchingQueryOptionsHook(
    { params }: { params?: SearchingQueryParams } = {},
    config: Partial<RequestConfig> & { client?: Client } = {}
) {
    const queryKey = searchingQueryKey(params);
    return queryOptions<
        SearchingQueryResponse,
        ResponseErrorConfig<Error>,
        SearchingQueryResponse,
        typeof queryKey
    >({
        queryKey,
        queryFn: async ({ signal }) => {
            return searching(
                { params: params },
                { ...config, signal: config.signal ?? signal }
            );
        },
    });
}

/**
 * {@link /api/search}
 */
export function useSearchingHook<
    TData = SearchingQueryResponse,
    TQueryData = SearchingQueryResponse,
    TQueryKey extends QueryKey = SearchingQueryKey
>(
    { params }: { params?: SearchingQueryParams } = {},
    options: {
        query?: Partial<
            QueryObserverOptions<
                SearchingQueryResponse,
                ResponseErrorConfig<Error>,
                TData,
                TQueryData,
                TQueryKey
            >
        > & { client?: QueryClient };
        client?: Partial<RequestConfig> & { client?: Client };
    } = {}
) {
    const { query: queryConfig = {}, client: config = {} } = options ?? {};
    const { client: queryClient, ...resolvedOptions } = queryConfig;
    const queryKey = resolvedOptions?.queryKey ?? searchingQueryKey(params);

    const query = useQuery(
        {
            ...searchingQueryOptionsHook({ params }, config),
            ...resolvedOptions,
            queryKey,
        } as unknown as QueryObserverOptions,
        queryClient
    ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
        queryKey: TQueryKey;
    };

    query.queryKey = queryKey as TQueryKey;

    return query;
}
