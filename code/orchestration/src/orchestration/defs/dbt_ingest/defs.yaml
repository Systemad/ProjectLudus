type: dagster_dbt.DbtProjectComponent

attributes:
    project: "{{ context.project_root }}/dbt"
    translation:
        group_name: igdb_dbt_transformations
        description: "Transforms data using dbt model {{ node.name }}"
