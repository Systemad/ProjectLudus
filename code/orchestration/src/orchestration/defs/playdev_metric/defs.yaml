type: dagster_dlt.DltLoadCollectionComponent

attributes:
  loads:
    - source: .metric_copy.metric_copy_source
      pipeline: .metric_copy.metric_copy_pipeline
      translation:
        group_name: metric_copy
        description: "Copy game metrics from source Postgres into igdb_source.game_metrics"
