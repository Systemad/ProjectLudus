version: 2
models:
    - name: bridge_game_publisher

      config:
          contract: { enforced: true }
          alias: "game_publisher"

      constraints:
          - type: primary_key
            columns: [game_id, company_id]

          - type: foreign_key
            columns: [game_id]
            to: ref('mart_games')
            to_columns: [id]

          - type: foreign_key
            columns: [company_id]
            to: ref('mart_companies')
            to_columns: [id]

      description: ""
      columns:
          - name: game_id
            data_type: bigint
            description: ""
          - name: company_id
            data_type: bigint
            description: ""
